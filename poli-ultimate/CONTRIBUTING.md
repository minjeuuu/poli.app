# Contributing to POLI

Thank you for your interest in contributing to POLI! 🎓

## Getting Started

1. Fork the repository
2. Clone your fork: `git clone https://github.com/YOUR_USERNAME/poli-app.git`
3. Create a branch: `git checkout -b feature/your-feature-name`
4. Make your changes
5. Test locally: `npm run dev`
6. Commit: `git commit -m 'Add your feature'`
7. Push: `git push origin feature/your-feature-name`
8. Open a Pull Request

## Development Setup

```bash
npm install
echo "VITE_API_KEY=sk-ant-your-key" > .env.local
npm run dev
```

## Code Style

- Use TypeScript for type safety
- Follow existing component structure
- Add comments for complex logic
- Keep components focused and reusable

## Testing Changes

Before submitting a PR:
- ✅ Test locally with `npm run dev`
- ✅ Ensure build works: `npm run build`
- ✅ Check for TypeScript errors
- ✅ Verify API calls work correctly

## Pull Request Guidelines

- Clear description of changes
- Reference any related issues
- Include screenshots for UI changes
- Ensure CI passes

## Questions?

Open an issue or discussion on GitHub!

Thanks for contributing! 🙌
